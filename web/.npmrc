# R2 升级（2026-08-29 · VP-024）：六包改为 npmjs.com 公开发布
#   @magicvr/schema-ui-*（公开 · 免凭据消费）——不再需要 GH Packages registry 映射
#   （历史：R1 · VP-023 曾映射 @magicvr → https://npm.pkg.github.com 认证消费）。
# 注意：若本机残留旧 GH Packages 映射（~/.npmrc），请移除以避免拉取私有版。